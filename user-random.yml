info:
  name: user-random
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/api/users/random
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
